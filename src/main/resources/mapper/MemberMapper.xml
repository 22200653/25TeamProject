<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.MemberMapper">

    <sql id="memberCols">
        id, userid, password, username, role, created_at AS createdAt
    </sql>

    <select id="findByUserid" resultType="model.Member">
        SELECT <include refid="memberCols"/>
        FROM members
        WHERE userid = #{userid}
    </select>

    <select id="findByUseridAndPassword" resultType="model.Member">
        SELECT <include refid="memberCols"/>
        FROM members
        WHERE userid = #{userid}
        AND password = #{password}
    </select>

    <insert id="insert" parameterType="model.Member" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO members (userid, password, username, role)
        VALUES (#{userid}, #{password}, #{username}, #{role})
    </insert>

</mapper>
