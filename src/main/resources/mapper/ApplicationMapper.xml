<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.ApplicationMapper">

    <select id="findByRecruitId" parameterType="int" resultType="Application">
        SELECT id, recruit_id, name, grade, major, message, created_at
        FROM application
        WHERE recruit_id = #{recruitId}
        ORDER BY id DESC
    </select>

    <select id="findById" parameterType="int" resultType="Application">
        SELECT id, recruit_id, name, grade, major, message, created_at
        FROM application
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="Application" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO application (recruit_id, name, grade, major, message)
        VALUES (#{recruitId}, #{name}, #{grade}, #{major}, #{message})
    </insert>

    <update id="update" parameterType="Application">
        UPDATE application
        SET name = #{name},
        grade = #{grade},
        major = #{major},
        message = #{message}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM application WHERE id = #{id}
    </delete>

</mapper>
