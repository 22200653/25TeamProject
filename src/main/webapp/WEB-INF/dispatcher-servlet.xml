<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <context:component-scan base-package="controller,service,repository,interceptor"/>
    <mvc:annotation-driven/>

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <import resource="mybatis-context.xml"/>

    <!-- ✅ 파일 업로드 (pom.xml에 commons-fileupload 필요) -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="UTF-8"/>
        <!-- 10MB -->
        <property name="maxUploadSize" value="10485760"/>
    </bean>

    <mvc:interceptors>

        <!-- ✅ 로그인 필요: 지원서 + 좋아요 -->
        <mvc:interceptor>
            <mvc:mapping path="/application/**"/>
            <mvc:mapping path="/recruit/like"/>
            <bean class="interceptor.AuthInterceptor"/>
        </mvc:interceptor>

        <!-- ✅ 관리자 필요 -->
        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/>
            <bean class="interceptor.AdminInterceptor"/>
        </mvc:interceptor>

    </mvc:interceptors>


</beans>
